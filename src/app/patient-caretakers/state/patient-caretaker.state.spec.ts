import {PatientCaretakerState} from './patient-caretaker.state';

describe('PatientCaretakerState', () => {
  it('should create an instance', () => {
    expect(new PatientCaretakerState()).toBeTruthy();
  });
});
