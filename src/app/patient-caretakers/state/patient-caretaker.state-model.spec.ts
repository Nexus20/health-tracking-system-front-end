import {PatientCaretakerStateModel} from './patient-caretaker.state-model';

describe('PatientCaretakerStateModel', () => {
  it('should create an instance', () => {
    expect(new PatientCaretakerStateModel()).toBeTruthy();
  });
});
