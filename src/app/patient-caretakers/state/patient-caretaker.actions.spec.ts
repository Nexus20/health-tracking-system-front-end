import {PatientCaretakerActions} from './patient-caretaker.actions';

describe('PatientCaretakerActions', () => {
  it('should create an instance', () => {
    expect(new PatientCaretakerActions()).toBeTruthy();
  });
});
